# Requirements inferred from gluejob_s3.py
# Note: `awsglue` is part of the AWS Glue runtime and is not installable via pip in the usual way.
# To run this script locally you can either run it inside an AWS Glue job, use the AWS Glue local docker image,
# or follow the aws-glue-libs manual installation from AWS Labs (not pip-installable in most cases).

pyspark
boto3
botocore
awswrangler

# Optional - testing / linting
pytest
